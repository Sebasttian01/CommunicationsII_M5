id: b_Mod_BPSK
label: b_Mod_BPSK_Zeroh
category: '[comdiguis]'

parameters:
-   id: Sps
    label: Samples per Symb
    dtype: int
    default: '8'
    hide: none

inputs:
-   label: in
    dtype: float
    vlen: 1

outputs:
-   label: out
    dtype: complex
    vlen: 1

templates:
    imports: 'from b_Mod_BPSK import b_Mod_BPSK  # grc-generated hier_block'
    make: "b_Mod_BPSK(\n    Sps=${ Sps },\n)"
    callbacks:
    - set_Sps(${ Sps })

documentation: 'Homero Ortega Boada. Universidad industrial de Santander

    Entrega una senal BPSK lista para ser entregada a un USRP, pero usando un zero
    order hold (osea que no usa filtro coseno alzado). Le entrada son unos o ceros
    tipo float

    /home/comu2_grupoc1/.local/state/gnuradio/b_Mod_BPSK.py'
grc_source: /home/comu2_grupoc1/CommunicationsII_M5/comdig_Lib_Bloques.3.8-master/b_Mod_BPSK.grc

file_format: 1
